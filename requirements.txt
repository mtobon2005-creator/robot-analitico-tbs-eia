streamlit[auth]>=1.52.2,<2
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
plotly==5.24.1
altair>=5.5.0,<6
python-dotenv==1.0.1
pytest==8.3.3

# Para datos reales de mercado (YFinanceProvider en src/data.py).
# No lo usan las pruebas automatizadas (siguen usando el fixture).
# Sin versión fija: Yahoo cambia su API con frecuencia y yfinance
# necesita actualizarse seguido para seguirle el paso.
yfinance
